apiVersion: jumpstarter.dev/v1alpha1
kind: Client
metadata:
  name: {{ .Values.app.name }}-build
  namespace: {{ .Values.jumpstarter.namespace }}
spec:
  username: "kubernetes:{{ .Values.app.name }}-build"
